import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  cardInfo: {
    padding: "1em",
    width: 180,
    display: "flex",
    justifyContent: "center",
  },
  letters: {
    fontSize: "1rem",
    textAlign: "left",
  },
});
