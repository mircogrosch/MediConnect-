import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  root: {
    minHeight: "100vh",
  },
  container: {
    height: "90vh",
    margin: "0 1em",
    alignItems: "center",
    display: "flex",
  },
});
