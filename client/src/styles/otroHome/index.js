import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  root: {
    height: "90vh",
    margin: "0 1em",
    alignItems: "flex-end",
    display: "flex",
  },
});
