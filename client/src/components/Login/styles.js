import {makeStyles} from '@material-ui/core'
const useStyles = makeStyles({
    root:{ 
        backgroundImage:"linear-gradient(to bottom, #b2dfdb, #93d7d0, #72d0c3, #4cc8b5, #00bfa5)"
    },
    textField: { 
        marginBottom:15, 
    },
})

export default useStyles;